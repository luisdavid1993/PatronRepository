using TT.AdoNetEntity;
//using TT.Dto;
using AutoMapper;

namespace TT.Repository.SpecificationMapper
{
    public static class SpecificationsMapper
    {
        //public static MapperConfiguration MiEntidadToDto { get { return new MapperConfiguration(cfg => cfg.CreateMap<UserDetails, UserDetailDto>().ForMember(d => d.IdUserDetail, opt => opt.MapFrom(src => src.Id))); } }
    }
}
