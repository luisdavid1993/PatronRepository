//using TT.Dto;
using TT.WebTab.Models;
//using AutoMapper;

namespace TT.WebTab.SpecificationMapper
{
    public static class SpecificationsMapper
    {
       // public static MapperConfiguration MiModeloToDto { get { return new MapperConfiguration(cfg => cfg.CreateMap<UserDetails, UserDetailDto>().ForMember(d => d.IdUserDetail, opt => opt.MapFrom(src => src.Id))); } }
    }
}
